spring:
  application:
    name: ValidaBoleto

  datasource:
    url: jdbc:postgresql://localhost:5432/ValidaBoleto
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

  flyway:
    enabled: true

